# 在线飞书与 BI 复核记录

## 基线与业务排除

- 用户指定的 `blkxo6iiWpcZgJhf` 是北京销售业务总览。全国对账继续检查同一 Base 的销售订单、订单明细、回款记录三张来源表，不能把北京视图当全国数据。
- 2026-09-12 17:40 至 17:45（北京时间），通过已登录飞书页面重新下载三张完整表，不使用 09-09 文件代替在线现状。三表顺序导出，不是跨表事务快照。
- 用户本轮明确：此前提到的 15 条数量为零的订单属于后来退款的记录，不录入系统。保留现有业务排除，不补录为有效订单，不算漏导；只保留已有原始导入和跳过记录用于审计。
- 此结论仅针对已确认的退款排除记录，不能推广为所有零数量、有回款或有异常的记录都可自动删除。

## 已确认结果

| 核对项 | 当前证据 | 结论与限制 |
| --- | --- | --- |
| 在线订单 | 2257 条，订单号无重复 | 系统报表有 2242 条；差集恰为 15 条已确认排除订单，没有额外系统订单 |
| 在线明细 | 3699 条，明细号无重复 | 09-09 文件有 3701 条；在线已删除两条明细，不应按旧文件补回 |
| 在线回款记录 | 984 条，回款编号无重复 | 存在一条未关联订单的 144 元回款，不能自动归入销售回款 |
| 原来提到的 156 元 | 一笔订单从 3 件、234 元改为 1 件、78 元，两个关联明细已删除 | 当前系统报表为 78 元，与当前在线一致，不是已证实漏导 |
| 原来提到的 205 元 | 一笔排除订单的已付、收款、待付已全部改为 0 | 205 元来自旧快照，撤回“当前漏收款”判断 |
| 城市、销售、客户归属 | 对齐 2242 个有效订单，去除名称首尾空白 | 唯一空归属订单在系统显示“未归属城市/未分配”，不是系统换错归属 |
| 订单级金额 | 当前在线导出与 16:04 系统报表逐单比较 | 应收、累计已收、待收没有超过 0.01 元的差异；仍有 7 个金额字段相差 0.01 元，未擅自抹平 |
| 北京视图 | 销售额 30925.00 元、累计收款 17723.30 元、待付 13201.70 元 | 在线导出按北京汇总与当前飞书看板一致 |

在线页面全国汇总显示销售 730067.52 元、回款 407864.13 元、待回款 322203.39 元，与本轮成功查询的 BI 总览一致。飞书 Excel 逐行已显示为两位小数，逐行相加分别为 730067.53、407864.13、322203.40 元。必须区分“原始精度汇总后显示”与“逐行显示值求和”；尚未取得在线单元格完整精度，不将一分钱差异直接定性为源数据错误或导入错误。

已关联的在线回款记录合计加订单初始已付，与订单收款合计逐单核对，在 0.01 元比较精度下无差异。未关联的 144 元没有擅自分摊，也没有推断成退款。

## 未完成验收

1. 系统最新下载复核：16:04 的系统业务报表已逐单比对；17:30 后的 BI 页面总额已观察。随后登录态失效，已请用户重新登录。不能将较早报表说成与 17:40 在线导出同时生成的快照。
2. 商品/SKU 完整对账：154 个订单的在线物理明细行数与系统行数不同。源码 `mergedSalesOrderLines` 按商品和 SKU 合并，所以行数少不能直接认定漏明细。按显示商品名去重后仍有 121 个订单待进一步检查 ERP 映射、规格、数量单位与商品金额。没有把这部分判为已通过。
3. 来源关联：在线有 13 条明细无法从关联订单字段解析到订单。要保留为独立待核对项，不能盲目拼接名称或猜测归属。
4. BI 对账状态：服务端目前某些来源不可比较的项目仍可能显示 `PASS`，原始导入快照与业务筛选范围也需统一。前端说明已更正，但后端“可比较”状态契约尚未修完，不把页面 PASS 当在线对账通过。
5. 在线持续变更：本轮是只读导出快照对账，没有建立在线改动自动检测或自动重导。后续需记录来源记录标识、修改时间、内容摘要与导入版本，识别新增、修改、删除和退款排除；不得用旧文件覆盖新的财务结果。

## 本轮代码改动

- 成本结构图 tooltip 对大金额使用万元、两位小数；小金额保持元，明细及 Excel 的精确金额不变。
- 历史导入新增“查看结果”，只调用执行结果 GET。可查原始行号、来源单号、跳过/失败/待处理原因，并支持状态筛选、搜索和分页，不触发重新导入。
- 大批次最多读取 10000 条结果，界面明确提示部分明细范围。待处理同时包含待执行和待映射。
- 对账抽屉明确“来源是已接收导入快照，并非当前在线飞书”，退款排除结合导入结果核对。这些说明不占经营总览主屏。

## 经营看板后续验收路径

这份对账记录不替代既有三层设计，也不宣称所有业务闭环已完成。

| 使用者要完成的工作 | 已有支持 | 仍需验收或补齐 |
| --- | --- | --- |
| 老板判断销售、资金和成本消耗 | 四个核心指标、趋势、成本消耗与结余、专题下钻 | 完整真实成本覆盖、预算目标、同期间口径；样例仅留在缺失费用项 |
| 运营报批方便面订货 | 城市分类、商品 SKU、订单核对、Excel 自选列，默认不分摊回款 | ERP 品牌/分类/SKU 映射与箱桶数量语义逐项对账；库存、在途、采购参考价真实关联 |
| 销售识别客户与催收动作 | 全国业绩、客户与风险下钻 | 个人数据范围、责任归属、跟进状态与时间、拜访转化窗口、目标配置 |
| 城市总管理当地业绩 | 城市筛选与经营专题、销售和商品维度 | 城市权限必须后端约束；历史责任销售与当前人员归属分开，目标和任职期联动 |

继续使用浅色紧凑图表画布；不增加角色卡片，不使用城市/库存散点，不恢复大色块树图或回款率横向进度榜。图表定位问题，明细承接核对和行动。

## 验证与数据安全

- 本轮 3 个相关测试文件、20 个测试通过；相关文件 ESLint、`vue-tsc` 与 Vite 构建通过。构建保留原有 Rollup 注释和大 chunk 告警。
- 已在真实页面读取历史批次 2257 条结果、筛选 15 条跳过记录，未触发重新导入。
- 未修改在线飞书，未执行同步、迁移、订单写入或退款写入。完整导出文件只留本机，不加入 Git。
- 登录失效后的最终页面/最新系统报表复验尚未完成；不以单元测试或服务健康检查代替业务验收。
