# 瑞盖优选供应链运营门户 —— 工程骨架验收报告

## 验收结果

| 检查项 | 结果 |
|---|---|
| Lint (eslint) | ✅ 通过，0 errors |
| TypeCheck (vue-tsc) | ✅ 通过，0 errors |
| Test (vitest) | ✅ 通过，1 passed |
| Build (vite build) | ✅ 通过，3.16s |

## 改动文件清单

### 配置层
- `package.json`
- `pnpm-lock.yaml`
- `tsconfig.json`
- `tsconfig.node.json`
- `vite.config.ts`
- `vitest.config.ts`
- `eslint.config.js`
- `.prettierrc`
- `.editorconfig`
- `.env` / `.env.example` / `.env.production`
- `.gitignore`
- `index.html`
- `README.md`

### 类型定义 (`src/types/`)
- `api.ts` — ApiResponse、PaginationParams、PaginatedData
- `router.ts` — 通过 declare module 扩展 vue-router RouteMeta
- `auth.ts` — LoginRequest、LoginResponse、UserInfo
- `index.ts` — 统一导出

### API 层 (`src/api/`)
- `core/client.ts` — Axios 实例（token/tenant_id/request_id 注入、统一解包、401 拦截）
- `core/error.ts` — 错误码映射 + 统一错误展示
- `core/index.ts` — 导出
- `mock/adapter.ts` — Mock handler 注册表
- `mock/handlers/auth.ts` — 登录/用户信息 Mock
- `mock/handlers/index.ts` — 聚合导出
- `mock/index.ts` — 导出

### 工具函数 (`src/utils/`)
- `token.ts` — Token 存取
- `request-id.ts` — trace id 生成

### 状态管理 (`src/stores/`)
- `auth.ts` — 认证 Store（login/logout/fetchUser/hasPermission/hasRole）
- `app.ts` — 应用 Store（侧边栏折叠、面包屑、当前租户）
- `permission.ts` — 权限路由 Store
- `index.ts` — 统一导出

### 路由 (`src/router/`)
- `routes.ts` — 8 个模块动态路由（总部、ERP、订单、销售、BI、人事、城市、代理）
- `permissionGuard.ts` — 路由守卫
- `index.ts` — Router 实例

### 指令 (`src/directives/`)
- `permission.ts` — v-permission 指令

### Composables
- `composables/useAuth.ts`

### 布局 & 组件
- `layouts/DefaultLayout.vue` — 侧边导航 + 顶栏 + 内容区
- `components/AppSidebar.vue`
- `components/AppTopbar.vue`

### 视图
- `views/login/LoginView.vue`
- `views/dashboard/DashboardView.vue`
- 8 个模块占位页面（hq, erp, order, sales, bi, hr, city, channel）
- `views/error/NotFoundView.vue`

### 入口
- `main.ts` / `App.vue`
- `tests/example.test.ts`

## 残余风险

1. **主 chunk 体积较大**（1,247 KB）。Element Plus 全部引入所致，后续使用 `build.rollupOptions.output.manualChunks` 拆分。
2. **Mock 数据未与 Axios 拦截器深度绑定**。当前只有注册表，后续可接入 `axios-mock-adapter` 或 MSW 实现请求级 Mock。
3. **权限路由动态过滤未接入**。`permissionStore.accessibleRoutes` 默认为空，需在登录后根据用户权限过滤并注入路由。
4. **vitest.config.ts 类型兼容**。`@vitejs/plugin-vue` v5 与 Vite 6 类型有差异，已排除 vue-tsc 检查，后续升级 plugin-vue 到 v6 解决。
5. **未配置 CI/CD**。
6. **设计 Token 覆盖度**。当前只覆盖基本色，后续可按需定制 Element Plus 全部组件变量。
